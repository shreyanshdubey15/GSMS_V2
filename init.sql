-- Initial database setup for Grocery Store V2
-- This file runs when the PostgreSQL container starts

-- Create database if it doesn't exist (handled by docker-compose environment variables)
-- The database and user are created by the POSTGRES_* environment variables in docker-compose.yml

-- You can add any additional database initialization here if needed